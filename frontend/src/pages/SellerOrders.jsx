import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import api from '../api/axios.js';
import '../CSS/AdminOrders.css';
export default function SellerOrders() { const [orders, setOrders] = useState([]); const [error, setError] = useState(''); useEffect(() => { api.get('/seller/orders').then((response) => setOrders(response.data)).catch((requestError) => setError(requestError.response?.data?.message || 'Unable to load seller orders.')); }, []); return <section className="page-container admin-orders-page"><header className="page-header"><p className="eyebrow">Seller workspace</p><h1 className="page-title">Orders</h1><p className="page-subtitle">Orders containing your products.</p></header>{error && <p className="feedback-error">{error}</p>}<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Order</th><th>Customer</th><th>Products</th><th>Revenue</th><th>Status</th><th>Date</th></tr></thead><tbody>{orders.map((order) => <tr key={order.id}><td><Link to={`/orders/${order.id}`}>#{order.id}</Link></td><td>{order.User?.name || 'Customer'}</td><td>{(order.OrderItems || []).map((item) => <div key={item.id}>{item.Product?.name} × {item.quantity}</div>)}</td><td>{(order.OrderItems || []).reduce((sum, item) => sum + Number(item.price) * item.quantity, 0).toFixed(2)} TND</td><td><span className={`status-badge status-${order.status}`}>{order.status}</span><small>{order.paymentStatus}</small></td><td>{new Date(order.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div></section>; }
